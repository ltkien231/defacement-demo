<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold text-gray-800 mb-2">University Login</h1>
            <p class="text-gray-600">Please sign in to continue</p>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-4">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="ml-3">
                        <p class="text-sm text-red-700"><%= error %></p>
                    </div>
                </div>
            </div>
        <% } %>

        <form action="/login" method="POST" class="space-y-6">
            <div>
                <% if (typeof error !== 'undefined' && error && !username) { %>
                    <div class="text-sm text-red-600 mb-1">Username is required</div>
                <% } %>
                <div class="relative">
                    <input type="text" 
                           id="username" 
                           name="username" 
                           class="block w-full px-3 py-2 border <%= typeof error !== 'undefined' && error && !username ? 'border-red-500' : 'border-gray-300' %> rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                           placeholder="Username">
                    <label for="username" class="absolute left-3 -top-2.5 bg-white px-1 text-sm text-gray-500"></label>
                </div>
            </div>
            
            <div>
                <% if (typeof error !== 'undefined' && error && !password) { %>
                    <div class="text-sm text-red-600 mb-1">Password is required</div>
                <% } %>
                <div class="relative">
                    <input type="password" 
                           id="password" 
                           name="password" 
                           class="block w-full px-3 py-2 border <%= typeof error !== 'undefined' && error && !password ? 'border-red-500' : 'border-gray-300' %> rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                           placeholder="Password">
                    <label for="password" class="absolute left-3 -top-2.5 bg-white px-1 text-sm text-gray-500"></label>
                </div>
            </div>

            <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                Login
            </button>
        </form>
    </div>
</body>
</html> 